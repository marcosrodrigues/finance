<%= form_for(@receivable) do |f| %>
  <% if @receivable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@receivable.errors.count, "error") %> prohibited this receivable from being saved:</h2>

      <ul>
      <% @receivable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :account_id %><br />
    <%= f.select :account_id, @accounts, :prompt => "Selecione..." %>
  </div>
  <div class="field">
    <%= f.label :favored_id %><br />
    <%= f.select :favored_id, @favoreds, :prompt => "Selecione..." %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :maturity %><br />
    <%= f.date_select :maturity, :order => [:day, :month, :year], :prompt => {:day => 'Dia', :month => 'Mês', :year => 'Ano'} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
